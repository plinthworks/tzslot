{
  "name": "ngx-zoneddatepicker-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p packages/core/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "temporal-polyfill": "^1.0.5"
  }
}
